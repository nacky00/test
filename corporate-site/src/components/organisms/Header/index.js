import React from 'react';

class Header extends React.Component {
    render() {
        return (
            <header style = {{ height: 60, background: "#ddd", padding: 24 }} >
                original header
            </header>
        );
    }
}

export default Header;